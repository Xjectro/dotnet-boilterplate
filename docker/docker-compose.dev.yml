services:
  cassandra:
    image: cassandra:4.1
    container_name: cassandra
    ports:
      - "9042:9042"
    environment:
      CASSANDRA_CLUSTER_NAME: "local-cluster"
      CASSANDRA_DC: "istanbul"
      CASSANDRA_RACK: "rack1"
    volumes:
      - cassandra_data:/var/lib/cassandra
    healthcheck:
      test: ["CMD", "cqlsh", "-e", "describe keyspaces"]
      interval: 30s
      timeout: 10s
      retries: 5
  redis:
    image: redis:7.2
    container_name: redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
  api:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: api-dev
    ports:
      - "5143:5143"
    environment:
      DOTNET_ENVIRONMENT: Development
      JWT_SETTINGS__SECRET: "a19b225eb281d6c5e09d0d329aa67fd1"
      JWT_SETTINGS__EXPIRY_MINUTES: "60"
      REDIS__HOST: "redis:6379"
      ALLOWED__HOSTS: "*"
      CASSANDRA__CONTACT_POINTS: "cassandra"
      CASSANDRA__PORT: "9042"
      CASSANDRA__DATACENTER: "istanbul"
      CASSANDRA__KEYSPACE: "default_keyspace"
      CASSANDRA__CONSISTENCY: "LocalQuorum"
      CASSANDRA__REPLICATION_CLASS: "SimpleStrategy"
      CASSANDRA__REPLICATION_FACTOR: "1"
    depends_on:
      redis:
        condition: service_healthy
      cassandra:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5143/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5

volumes:
  cassandra_data:
  redis_data:
